name: Snapshot Standalone LS

on:
  workflow_dispatch:
  workflow_call:

permissions:
  contents: write

env:
  AWS_ACCESS_KEY_ID: ${{ secrets.CDN_S3_ACCESS_KEY }}
  AWS_SECRET_ACCESS_KEY: ${{ secrets.CDN_S3_SECRET_KEY  }}
  AWS_DEFAULT_REGION: us-east-1
  AWS_ENDPOINT_URL_S3:  ${{ secrets.CDN_S3_ENDPOINT }}
  AWS_S3_BUCKET: ${{ secrets.CDN_BUCKET }}/spring-tools
  DOWNLOAD_URL_ROOT: https://cdn.spring.io/spring-tools

jobs:
  build-standalone-ls:
    runs-on: ubuntu-latest
    name: Build Standalone LS Snapshot
    steps:
      - name: Checkout code
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # 7.0.1
      - name: Set up JDK 25
        uses: actions/setup-java@03ad4de0992f5dab5e18fcb136590ce7c4a0ac95 # 5.6.0
        with:
          java-version: '25'
          distribution: 'temurin'
      - name: Build and Package
        id: build-jar
        run: |
          ${{ github.workspace }}/.github/scripts/build-standalone-ls.sh snapshot
          ls ./standalone-ls-dist
      - name: Upload Standalone JAR
        id: upload-release
        run: |
          jar_file="spring-boot-language-server-standalone-exec.jar"
          sha_file="$jar_file.sha256"
          s3_path=snapshot/language-server/spring-boot
          echo "S3 path: ${s3_path}"
          aws s3 cp ./standalone-ls-dist/$jar_file s3://$AWS_S3_BUCKET/$s3_path/$jar_file --no-progress --checksum-algorithm CRC32
          aws s3 cp ./standalone-ls-dist/$sha_file s3://$AWS_S3_BUCKET/$s3_path/$sha_file --no-progress --checksum-algorithm CRC32
      - name: Update plugin.json for Snapshot
        id: update-plugin
        run: |
          git config --global user.name "@spring-projects/spring-ide-admin"
          git config --global user.email "spring-ide-admin@users.noreply.github.com"
          
          version=$(cat ./standalone-ls-dist/version.txt)
          
          # Create or reset a local branch to the current commit (which is what triggered the action)
          git checkout -B claude-snapshot
          
          # Keep the Claude, portable, and Codex manifests on the same server version.
          for manifest in claude-plugins/spring-tools/{.claude-plugin/plugin.json,plugin.json,.codex-plugin/plugin.json}; do
            tmp=$(mktemp)
            jq --arg v "$version" '.version = $v' "$manifest" > "$tmp" && mv "$tmp" "$manifest"
            git add "$manifest"
          done
          
          git commit -m "Update Spring Tools plugin version to $version"
          
          # Push to the remote claude-snapshot branch forcefully.
          # If it doesn't exist, this creates it. If it does exist, it completely overwrites it
          # to exactly match the current main + this single version bump commit.
          git push origin claude-snapshot --force
          
          echo "sha=$(git rev-parse HEAD)" >> $GITHUB_OUTPUT
      - name: Update Marketplace JSON
        run: |
          aws s3 cp s3://$AWS_S3_BUCKET/snapshot/claude-plugins/marketplace.json ./marketplace.json || echo '{"name": "spring-tools-snapshots", "description": "Bleeding-edge snapshot builds for Spring Tools Claude Code plugins.", "owner": {"name": "Spring Tools"}, "plugins": []}' > ./marketplace.json
          
          version=$(cat ./standalone-ls-dist/version.txt)
          sha="${{ steps.update-plugin.outputs.sha }}"
          
          tmp=$(mktemp)
          jq --arg name "spring-tools" \
             --arg version "$version" \
             --arg sha "$sha" \
             '
             .plugins |= if map(.name == $name) | any then
                 map(if .name == $name then .version=$version | .source.sha=$sha | .source.ref="claude-snapshot" else . end)
             else
                 . + [{
                    "name": $name, 
                    "version": $version, 
                    "source": {
                      "source": "git-subdir",
                      "url": "https://github.com/spring-projects/spring-tools.git",
                      "path": "claude-plugins/spring-tools",
                      "ref": "claude-snapshot",
                      "sha": $sha
                    }, 
                    "description": "Spring Boot Language Server", 
                    "author": {"name": "Spring Tools"}
                 }]
             end
             ' ./marketplace.json > "$tmp" && mv "$tmp" ./marketplace.json
             
          cat ./marketplace.json
          aws s3 cp ./marketplace.json s3://$AWS_S3_BUCKET/snapshot/claude-plugins/marketplace.json --no-progress --checksum-algorithm CRC32
